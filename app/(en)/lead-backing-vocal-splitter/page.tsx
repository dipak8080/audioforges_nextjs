import { UseCasePage, metadataForUseCase } from "@/components/studio/UseCasePage";

export const metadata = metadataForUseCase("lead-backing-vocal-splitter");

export default function Page() {
  return <UseCasePage slug="lead-backing-vocal-splitter" />;
}